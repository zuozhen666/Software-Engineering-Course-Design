## 前后端关系
1. 不分离：浏览器（前端）+服务器（后端），在前端读取请求，后端读取数据，渲染好后发给前端，即前端显示的是后端渲染好的
2. 分离：后端不再控制前端的效果展示，浏览器（静态页面的文本文件）-> 后端，例如nginx提供部署 -> 返回浏览器（前端），所以后端用来专门提供数据，拿到数据直接转化为json字符串，后端接口可以复用，只需要提供网址，
3. 前后端分离越来越流行：PC、手机app等众多产品用户都可以访问，后端减轻了开发的压力，爬虫在解析的时候可以建立数据，前端模版，前端框架
